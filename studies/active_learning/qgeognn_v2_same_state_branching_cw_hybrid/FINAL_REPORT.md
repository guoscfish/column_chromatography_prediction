# Same-state pilot — selector audit complete, outcomes pending

The scientific comparison is exact Center/Width-LCMD, Kernel-IVR, and Gradient-MaxDet at eight shared historical CW/Hybrid source states. Historical Hybrid is retained only as a source trajectory and source-identity regression control. The former gradient-norm proxy is removed.

This file is a result placeholder. No pilot fit, branch return, test metric, oracle headroom, or controller recommendation has been produced by the implementation audit. Planned execution remains 24 branches / 48 scratch fits, with test truth behind the global branch freeze.

Read `IMPLEMENTATION_AUDIT.md`, `decision.json`, and the selection regression CSVs for implementation evidence. Exact selection identity establishes method fidelity; proposal divergence alone does not establish adaptive benefit.

After separately authorized execution, report short-AULC relative to always-CW, local-oracle gain, source/stage dependence, and whether an independently preregistered replication is justified. Do not interpret these reused seeds as independent confirmation.
