# HIER Center/Width semantic repair

This isolated study audits the dimensional semantics of `HIER_CW_V2`, proves
exact nesting against historical HIER, compares fairly tuned shared and
separate column-deviation shrinkage, isolates the endpoint-aligned objective,
and re-audits only the corrected FULL128 joint model. Historical artifacts and
implementations remain unchanged. Active Learning is out of scope.
